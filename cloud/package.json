{
  "name": "quill-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Quill Cloud — Hono service on Railway, tiered routing (Gemini vision / OpenAI text) with per-device daily quota",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "test": "node --experimental-strip-types --test src/server.test.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14",
    "ioredis": "^5.4.1",
    "tsx": "^4.19.2"
  }
}
