{
  "name": "quill-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Quill Cloud — Cloudflare Worker proxying Gemini with per-device daily quota",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^3.86.0"
  }
}
